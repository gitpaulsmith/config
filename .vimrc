" Use visual bell instead of the audible bell.
set vb
" Turn off the visual bell.
set t_vb=

" Spaces and tabs
" Show existing tabs as 4 space wide.
set tabstop=4
" The number of spaces a tab counts for when editing.
set softtabstop=4
" On pressing tab, insert 4 spaces.
set expandtab
" When indenting with '>', use 4 spaces.
set shiftwidth=4
filetype plugin indent on
set autoindent

" UI Layout
" Show line numbers.
set number
" Show the command in the bottom bar.
set showcmd
" Highlight matching [{()}].
set showmatch
" Show columns
set ruler

" Searching
" Search as characters are entered.
set incsearch

